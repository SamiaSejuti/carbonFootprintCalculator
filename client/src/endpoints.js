
const BASE_URL = 'https://nodejs-api-fit5120.azurewebsites.net/api';

export const endpoints = {
    calculate: `${BASE_URL}/calculate`,
    compare: `${BASE_URL}/carbonemissiondata`,
    location: `${BASE_URL}/location`,
    authenticate: `${BASE_URL}/authenticate`
};
